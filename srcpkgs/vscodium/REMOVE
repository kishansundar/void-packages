if [ "$UPDATE" = "no" ]; then
    case "$ACTION" in
        post)
            if [ -d usr/lib/codium ]; then
		        rm -rf usr/lib/codium
            fi
            if [ -d usr/share/codium ]; then
                rm -rf usr/share/codium
            fi
        ;;
    esac
fi
